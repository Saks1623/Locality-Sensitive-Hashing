# Locality-Sensitive-Hashing
C++ implementations of Locality Sensitive Hashing (LSH) for approximate nearest neighbor search. Supports real-valued vectors (cosine similarity) and binary vectors (Hamming distance). Uses randomized hashing to group similar items, enabling fast similarity search in high-dimensional data.
